import 'package:flutter/material.dart';

class MyPointsFragment extends StatelessWidget {
  const MyPointsFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
    );
  }
}
