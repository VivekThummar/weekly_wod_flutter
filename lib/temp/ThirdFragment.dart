import 'package:flutter/material.dart';

class ThirdFragment extends StatelessWidget {
  const ThirdFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Hello Fragment 3"),
    );
  }
}
